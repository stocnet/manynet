% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/measure_attributes.R
\name{measure_attributes_ties}
\alias{measure_attributes_ties}
\alias{tie_attribute}
\alias{tie_weights}
\alias{tie_signs}
\alias{tie_is_twomode}
\title{Describing attributes of ties in a network}
\usage{
tie_attribute(.data, attr_name)

tie_weights(.data)

tie_signs(.data)

tie_is_twomode(.data)
}
\arguments{
\item{.data}{An object of a \code{{manynet}}-consistent class:
\itemize{
\item adjacency or incidence \code{matrix} from \code{{base}} R
\item edgelist \code{data.frame} from \code{{base}} R or \code{tbl/tbl_df} from \code{{tibble}}
\item stocnet \code{stocnet}, from the \code{{manynet}} package
\item igraph \code{igraph}, from the \code{{igraph}} package
\item network \code{network}, from the \code{{network}} package
\item tidygraph \code{tbl_graph}, from the \code{{tidygraph}} package
}}

\item{attr_name}{Character string naming a nodal attribute.
The attribute itself may be a logical mark, numeric measure, or character membership vector.}
}
\description{
These functions extract certain attributes from network data:
\itemize{
\item \code{tie_attribute()} returns an attribute's values for the ties in a network.
\item \code{tie_weights()} returns the weights of the ties in a network.
\item \code{tie_signs()} returns the signs of the ties in a network.
\item \code{tie_is_twomode()} returns whether each tie in a network is a cross-mode tie.
}

These functions are also often used as helpers within other functions.
\verb{tie_*()} always return vectors the same length
as the number of ties in the network, respectively.
}
\examples{
tie_attribute(ison_algebra, "task_tie")
tie_weights(to_mode1(ison_southern_women))
tie_signs(to_uniplex(fict_marvel,"relationship"))
tie_is_twomode(fict_actually)
}
\seealso{
Other measures:
\code{\link{measure_attributes_nodes}},
\code{\link{measure_dims}}
}
\concept{measures}
