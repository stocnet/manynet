% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modif_from.R
\name{modif_from_parts}
\alias{modif_from_parts}
\alias{from_subgraphs}
\alias{from_egos}
\title{Joining parts of a network}
\usage{
from_subgraphs(netlist)

from_egos(netlist)
}
\arguments{
\item{netlist}{A list of network, igraph, tidygraph, matrix, or edgelist
objects.}
}
\value{
An object of the same class as the function was given,
modified as explained in the function description, details, or section.
Functions that split a network return a list of such objects.
}
\description{
These functions join a list of networks that are each a part of one
network, holding some of its nodes and the ties among them,
back into that one network.
Each reverses one of the \code{to_*s()} functions that splits a network into
parts.
\itemize{
\item \code{from_subgraphs()} joins a list of subgraphs, as \code{to_subgraphs()}
returns, into one network.
\item \code{from_egos()} joins a list of ego networks, as \code{to_egos()} returns,
into one whole network.
}

A node or a tie that more than one part holds is held once in the result.

Every function here returns the class of the networks in the list:
a 'stocnet' from 'stocnet's, a 'tbl_graph' from 'tbl_graph's,
an 'igraph' from 'igraph's, a 'network' from 'network's,
and a single matrix from matrices, since the parts are all of one network.

To stack networks that each hold the same nodes, one for each moment,
layer, or reporter, see \code{\link[=from_times]{from_times()}} and the other functions there.
}
\examples{
ison_adolescents |>
  mutate(unicorn = sample(c("yes", "no"), 8, replace = TRUE)) |>
  to_subgraphs(attribute = "unicorn") |>
  from_subgraphs()
ison_adolescents |>
  to_egos() |>
  from_egos()
}
\seealso{
Other modifications:
\code{\link{modif_backbone}},
\code{\link{modif_concepts}},
\code{\link{modif_direction}},
\code{\link{modif_from_ternary}},
\code{\link{modif_labels}},
\code{\link{modif_levels}},
\code{\link{modif_miss}},
\code{\link{modif_motifs}},
\code{\link{modif_paths}},
\code{\link{modif_permutation}},
\code{\link{modif_plexity}},
\code{\link{modif_project}},
\code{\link{modif_proximity}},
\code{\link{modif_scope}},
\code{\link{modif_split}},
\code{\link{modif_weight}}
}
\concept{modifications}
