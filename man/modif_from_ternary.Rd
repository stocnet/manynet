% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modif_from.R
\name{modif_from_ternary}
\alias{modif_from_ternary}
\alias{from_waves}
\alias{from_slices}
\alias{from_times}
\alias{from_layers}
\alias{from_ties}
\alias{from_reporters}
\title{Joining networks along a third dimension}
\usage{
from_waves(netlist)

from_slices(netlist, remove.duplicates = FALSE)

from_times(netlist)

from_layers(..., layer_names)

from_ties(..., layer_names)

from_reporters(netlist)
}
\arguments{
\item{netlist}{A list of network, igraph, tidygraph, matrix, or edgelist
objects, over the same nodes.}

\item{remove.duplicates}{Should duplicates be removed?
By default FALSE.
If TRUE, duplicated edges are removed.}

\item{...}{Two or more networks over the same nodes to be merged,
or a single list of such networks, such as that returned by \code{to_layers()}.
Nodes are matched by name where the networks are labelled,
and by position where they are not and are the same size.}

\item{layer_names}{A character vector of names for the different network objects,
if not already named within the list.}
}
\value{
An object of the same class as the function was given,
modified as explained in the function description, details, or section.
Functions that split a network return a list of such objects.
}
\description{
These functions join a list of networks that each hold the same nodes,
one network for each moment, layer, or reporter, into one network.
Each tie then records the network it came from in one of its columns:
\code{time} for a moment, \code{layer} for a layer, and \code{by} for a reporter.
Each reverses one of the \code{to_*s()} functions that splits a network along
such a dimension.
\itemize{
\item \code{from_times()} joins a list of the network at each moment, as
\code{to_times()} returns, back into one network that records time.
This is where new work on rejoining a network over time belongs;
\code{from_waves()} and \code{from_slices()} are the older, form-specific spellings.
\item \code{from_waves()} joins a list of network waves into a longitudinal network.
\item \code{from_slices()} joins a list of time slices of a network into
a dynamic network.
\item \code{from_layers()} joins several networks over the same nodes into one
multiplex network, keeping each as its own layer.
\code{from_ties()} is an alias.
Where \code{to_layers()} splits a multiplex network into its layers,
\code{from_layers()} reassembles them.
To combine the networks' tie values into a single value per dyad instead,
use \code{to_aggregated()} on the result, or \code{join_ties()} for two networks.
\item \code{from_reporters()} joins a list of the networks that each reporter
reported, named after the reporters, into one cognitive social
structure (see \code{\link[=is_cognitive]{is_cognitive()}}).
}

Every function here returns the class of the networks in the list:
a 'stocnet' from 'stocnet's, a 'tbl_graph' from 'tbl_graph's,
an 'igraph' from 'igraph's, a 'network' from 'network's.
A list of matrices gives a three-dimensional array, with one slice for
each matrix, named after the list, since a matrix has no column in which
to record the moment, layer, or reporter of a tie.
\code{as_stocnet()} reads such an array back, once told what its slices hold.

A tie that a network records as missing stays missing from that network
alone, so a node that did not report at one moment, or in one layer, is
recorded as not reporting there and nowhere else.

To join networks that are each a part of one network, holding some of its
nodes, see \code{\link[=from_subgraphs]{from_subgraphs()}} and \code{\link[=from_egos]{from_egos()}}.
}
\details{
\code{from_times()} rejoins what \code{\link[=to_times]{to_times()}} returns, stamping each network's
ties with the moment it names before binding them, and so inverts it for
a network that stamps its moments. Where the networks carry the interval
each tie lasts over, the moment is already in those ties, so they are
bound and deduplicated rather than stamped again.

\code{from_reporters()} takes the reporter of each network from the names of
the list, which may be the labels of the nodes or their positions.
Where the list is not named, the networks are taken to be the reports of
the first nodes, in order.
}
\examples{
ison_adolescents |>
  mutate_ties(wave = sample(1:4, 10, replace = TRUE)) |>
  to_waves(attribute = "wave") |>
  from_waves()
ison_adolescents |>
  mutate_ties(time = 1:10, increment = 1) |> 
  add_ties(c(1,2), list(time = 3, increment = -1)) |> 
  to_slices(slice = c(5,7)) |>
  from_slices()
from_times(to_times(ison_tailorshop))
marriage <- to_uniplex(ison_florentine, "marriage")
business <- to_uniplex(ison_florentine, "business")
from_layers(marriage = marriage, business = business)
reports <- list(A = create_ring(4), B = create_star(4))
from_reporters(lapply(reports, add_node_attribute, "name", LETTERS[1:4]))
}
\seealso{
Other modifications:
\code{\link{modif_backbone}},
\code{\link{modif_direction}},
\code{\link{modif_from_parts}},
\code{\link{modif_labels}},
\code{\link{modif_levels}},
\code{\link{modif_miss}},
\code{\link{modif_motifs}},
\code{\link{modif_paths}},
\code{\link{modif_permutation}},
\code{\link{modif_plexity}},
\code{\link{modif_project}},
\code{\link{modif_proximity}},
\code{\link{modif_scope}},
\code{\link{modif_split}},
\code{\link{modif_weight}}
}
\concept{modifications}
