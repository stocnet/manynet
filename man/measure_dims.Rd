% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/measure_properties.R
\name{measure_dims}
\alias{measure_dims}
\alias{net_nodes}
\alias{net_modes}
\alias{net_ties}
\alias{net_layers}
\alias{layer_ties}
\alias{net_waves}
\alias{net_times}
\alias{mode_nodes}
\alias{net_dims}
\title{Describing network dimensions}
\usage{
net_nodes(.data)

net_modes(.data)

net_ties(.data)

net_layers(.data)

layer_ties(.data)

net_waves(.data)

net_times(.data)

mode_nodes(.data)

net_dims(.data)
}
\arguments{
\item{.data}{An object of a \code{{manynet}}-consistent class:
\itemize{
\item adjacency or incidence \code{matrix} from \code{{base}} R
\item edgelist \code{data.frame} from \code{{base}} R or \code{tbl/tbl_df} from \code{{tibble}}
\item stocnet \code{stocnet}, from the \code{{manynet}} package
\item igraph \code{igraph}, from the \code{{igraph}} package
\item network \code{network}, from the \code{{network}} package
\item tidygraph \code{tbl_graph}, from the \code{{tidygraph}} package
}}
}
\value{
\verb{net_*()} functions always relate to the overall graph or network,
usually returning a scalar.
\code{mode_nodes()} returns an integer of the number of nodes in a one-mode network,
or two integers representing the number of nodes in each nodeset
in the case of a two-mode network.
\code{layer_ties()} returns an integer of the number of ties in a single-layer
network, or one integer per layer (in \code{layer_names()} order)
in the case of a multiplex network.
}
\description{
These functions extract certain attributes from given network data:
\itemize{
\item \code{net_nodes()} returns the total number of nodes (of any mode) in a network.
\item \code{net_ties()} returns the number of ties in a network.
\item \code{mode_nodes()} returns the dimensions of a network in a vector
as long as the number of modes in the network.
\item \code{net_layers()} returns the number of layers in a multiplex network.
\item \code{layer_ties()} returns the number of ties in a vector
as long as the number of layers in the network.
\item \code{net_waves()} returns the number of waves a panel network records,
see \code{\link[=is_longitudinal]{is_longitudinal()}}. A network that is not a panel has one wave.
\item \code{net_times()} returns the number of distinct moments a network records,
however it records them: the waves of a panel, the events of a dynamic
network, or the moments an interval network begins and ends a tie at.
See the Time section of \code{\link[=to_time]{to_time()}}.
}

These functions are also often used as helpers within other functions.
}
\examples{
net_nodes(ison_southern_women)
net_modes(ison_southern_women)
net_ties(ison_southern_women)
net_layers(ison_southern_women)
layer_ties(fict_marvel)
net_waves(ison_monks)
net_times(irps_wwi)
mode_nodes(ison_southern_women)
mode_nodes(to_mode1(ison_southern_women))
}
\seealso{
Other measures:
\code{\link{measure_attributes_nodes}},
\code{\link{measure_attributes_ties}}
}
\concept{measures}
