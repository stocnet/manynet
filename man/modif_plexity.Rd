% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modif_plexity.R
\name{modif_plexity}
\alias{modif_plexity}
\alias{to_anti}
\alias{to_simplex}
\alias{to_uniplex}
\alias{to_layer}
\alias{to_flat}
\title{Modifying network complexity}
\usage{
to_anti(.data)

to_simplex(.data)

to_uniplex(.data, layer, tie)

to_layer(.data, layer, tie)

to_flat(.data, rule = c("max", "min", "mean", "sum", "product"))
}
\arguments{
\item{.data}{An object of a \code{{manynet}}-consistent class:
\itemize{
\item adjacency or incidence \code{matrix} from \code{{base}} R
\item edgelist \code{data.frame} from \code{{base}} R or \code{tbl/tbl_df} from \code{{tibble}}
\item stocnet \code{stocnet}, from the \code{{manynet}} package
\item igraph \code{igraph}, from the \code{{igraph}} package
\item network \code{network}, from the \code{{network}} package
\item tidygraph \code{tbl_graph}, from the \code{{tidygraph}} package
}}

\item{layer}{Character string naming one of the layers, or tie types,
in the network, i.e. one of those returned by \code{layer_names()},
to which the network should be reduced.
Where a network holds no tie types, it is already uniplex
and is returned unchanged.}

\item{tie}{Deprecated name for \code{layer}, retained for one version.}

\item{rule}{How the networks' tie values are reconciled into a single value
per dyad. A dyad tied in only one of the networks counts as untied in the
others, which is what makes rules such as "min" and "product" meaningful.
\itemize{
\item "max" (the default) takes the largest of the values,
so that a tie in any of the networks is a tie in the result.
This is the union of the networks.
\item "min" takes the smallest, so that only ties present in all of them
survive. This is their intersection.
\item "mean" averages the values, treating the networks as several readings
of one underlying relationship.
\item "sum" adds them, so that ties reinforce one another.
\item "product" multiplies them, so that a tie survives only where every
network records one, and strong ties are rewarded disproportionately.
}

Missing values propagate rather than being ignored, so that a dyad
unobserved in any of the networks is unobserved in the result.
Use \code{impute_ties()} first to state a different assumption.}
}
\value{
An object of the same class as the function was given,
modified as explained in the function description, details, or section.
Functions that split a network return a list of such objects.
}
\description{
These functions reformat manynet-consistent data.
\itemize{
\item \code{to_anti()} reformats network data into its complement, where only ties \emph{not} present in the original network
are included in the new network.
\item \code{to_simplex()} reformats complex network data, containing loops, to simplex network data, without any loops.
\item \code{to_uniplex()} reformats multiplex network data to a single type of tie.
\code{to_layer()} is an alias, using the layer-based vocabulary of
\code{layer_names()}, \code{net_layers()}, and \code{to_layers()}.
Use \code{to_layers()} to split a network into all of its layers at once.
\item \code{to_flat()} reduces multiplex network data to a single relation by
combining the values of all its layers, dyad by dyad, according to a rule.
Where \code{to_uniplex()} selects one layer and discards the rest,
\code{to_flat()} retains what every layer records.
}

If the format condition is not met,
for example \code{to_undirected()} is used on a network that is already undirected,
the network data is returned unaltered.
No warning is given so that these functions can be used to ensure conformance.

Unlike the \verb{as_*()} group of functions,
these functions always return the same class as they are given,
only transforming these objects' properties.
}
\details{
Not all functions have methods available for all object classes.
Below are the currently implemented S3 methods:

\if{html}{\out{<div class="sourceCode">}}\preformatted{Warning in utils::.S3methods(f): generic function 'to_layer' dispatches methods
for generic 'to_uniplex'
Warning in utils::.S3methods(f): generic function 'to_layer' dispatches methods
for generic 'to_uniplex'
           data.frame default igraph matrix stocnet tbl_graph
to_anti             *       *      *      *                 *
to_flat                     *                               *
to_simplex          *       *      *      *       *         *
to_uniplex                  2                               2
}\if{html}{\out{</div>}}
}
\examples{
to_anti(ison_southern_women)
as_tidygraph(create_filled(5)) |>
  mutate_ties(type = sample(c("friend", "enemy"), 10, replace = TRUE)) |>
  to_uniplex("friend")
to_flat(ison_florentine, rule = "sum")
}
\seealso{
Other modifications:
\code{\link{modif_backbone}},
\code{\link{modif_direction}},
\code{\link{modif_from}},
\code{\link{modif_labels}},
\code{\link{modif_levels}},
\code{\link{modif_miss}},
\code{\link{modif_motifs}},
\code{\link{modif_paths}},
\code{\link{modif_permutation}},
\code{\link{modif_project}},
\code{\link{modif_proximity}},
\code{\link{modif_scope}},
\code{\link{modif_split}},
\code{\link{modif_weight}}
}
\concept{modifications}
