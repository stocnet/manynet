% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_collect.R
\name{make_collect}
\alias{make_collect}
\alias{collect_cran}
\alias{collect_pkg}
\title{Making networks of inter- and intra-package dependencies}
\source{
https://www.r-bloggers.com/2016/01/r-graph-objects-igraph-vs-network/

Inspired by Jakob Gepp's \code{helfRlein::get_network()},
https://github.com/STATWORX/helfRlein/blob/master/R/get_network.R
}
\usage{
collect_cran(
  pkg = "all",
  dependencies = c("Depends", "Imports", "LinkingTo"),
  max_dist = Inf,
  direction = c("out", "in", "all")
)

collect_pkg(dir = getwd(), external = FALSE)
}
\arguments{
\item{pkg}{A character vector of one or more package names,
from which dependencies are collected.
By default "all", which collects the dependencies among all the packages
currently available on CRAN.}

\item{dependencies}{A character vector naming the dependency fields to
collect, from "Depends", "Imports", "LinkingTo", "Suggests",
and "Enhances".
By default \code{c("Depends", "Imports", "LinkingTo")},
the dependencies that must be installed alongside a package.}

\item{max_dist}{The maximum number of steps from \code{pkg} to collect.
By default infinite, i.e. the whole dependency closure.}

\item{direction}{Whether to collect the packages that \code{pkg} depends upon,
"out" by default, the packages that depend upon \code{pkg}, "in",
or both, "all".}

\item{dir}{Character string with the path of the directory in which to
look for R scripts.
By default the current working directory.
Where \code{dir} holds a DESCRIPTION file and an R folder, as a package does,
the R folder is searched.}

\item{external}{Logical.
Where TRUE, calls to functions that are not defined in \code{dir},
such as those from other packages, are included as nodes too.
By default FALSE, since these are numerous and rarely of interest.}
}
\value{
A \code{tidygraph} object representing the network of package dependencies
or function dependencies in a package.
}
\description{
These functions create networks of the dependencies between or within
R packages:
\itemize{
\item \code{collect_cran()} creates a network of the dependencies among the packages
available on CRAN.
It reads the \code{Depends}, \code{Imports}, \code{LinkingTo}, \code{Suggests},
and \code{Enhances} fields of each package's DESCRIPTION file,
and creates a network in which the nodes are packages
and the ties are dependencies of a given type.
\item \code{collect_pkg()} creates a network of the dependencies among the functions
defined in a directory of R scripts.
It uses R's own parser to establish where each function is defined
and which functions it calls,
and creates a network in which the nodes are functions
and the ties are calls.
}
}
\details{
Dependency networks grow quickly, and are most useful once scoped.
\code{collect_cran()} therefore collects only the \code{Depends}, \code{Imports},
and \code{LinkingTo} fields by default, since these are the dependencies that
must be installed alongside a package, as in \code{utils::install.packages()}.
Adding \code{Suggests} grows the dependency closure of a package by
two orders of magnitude.
For the same reason, \code{collect_pkg()} collects only calls to the functions
defined in the directory by default.

Both return networks that can be scoped further using, for example,
\code{\link[=to_ego]{to_ego()}}, \code{\link[=to_uniplex]{to_uniplex()}}, \code{\link[=to_giant]{to_giant()}}, \code{\link[=to_no_isolates]{to_no_isolates()}},
\code{\link[=to_blocks]{to_blocks()}}, or \code{\link[=to_subgraph]{to_subgraph()}}.

\code{collect_cran()} relies on \code{utils::available.packages()},
which caches the repository index for an hour by default.
Set \code{options(max.repo.cache.age = )} for a fresher or staler snapshot.

Note that these functions are not as actively maintained as others
in the package, so please let us know if any are not currently working
for you or if there are missing import routines
by \href{https://github.com/stocnet/manynet/issues}{raising an issue on Github}.
}
\examples{
\dontrun{
# The packages {manynet} depends upon, directly and indirectly:
collect_cran("manynet")
# The packages that depend directly upon {manynet}:
collect_cran("manynet", direction = "in", max_dist = 1)
}
\dontrun{
# The network of calls among the functions in the working directory:
collect_pkg()
# Collapsed onto generics, where the directory is a package:
# to_blocks(collect_pkg(), node_attribute(collect_pkg(), "generic"))
}
}
\seealso{
\code{\link[=to_ego]{to_ego()}}, \code{\link[=to_uniplex]{to_uniplex()}}, \code{\link[=to_no_isolates]{to_no_isolates()}}

Other makes:
\code{\link{make_create}},
\code{\link{make_ego}},
\code{\link{make_explicit}},
\code{\link{make_learning}},
\code{\link{make_play}},
\code{\link{make_random}},
\code{\link{make_read}},
\code{\link{make_stochastic}},
\code{\link{make_write}}
}
\concept{makes}
